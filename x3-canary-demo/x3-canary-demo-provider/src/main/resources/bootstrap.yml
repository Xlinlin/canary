server:
  port: 9180

xiaogj:
  x3:
    canary:
      enabled: true

spring:
  application:
    name: canary-provider
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
        server-addr: http://192.168.0.92:8848/
        metadata:
          x3_version: v1